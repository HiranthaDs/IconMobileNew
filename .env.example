# Change these before using the application on a shared network.
POS_PASSWORD=ICONM@2026
ADMIN_PASSWORD=ADMIN@2026
WHOLESALE_PASSWORD=ADMIN@WS

# Optional. If unset, the backend creates a private persistent signing secret.
# SESSION_SECRET=replace-with-a-long-random-secret

ICON_HOST=0.0.0.0
ICON_PORT=8000
ICON_BACKUP_DIR=_backups
SESSION_HOURS=12
MAX_REQUEST_BYTES=5242880
MAX_RESTORE_BYTES=536870912

# Only needed if the frontend is deployed as a SEPARATE origin/service from
# this API (e.g. a standalone static site). Comma-separated list of exact
# frontend origins, no trailing slash, no wildcards.
# ALLOWED_ORIGINS=https://icon-mobile-backend.onrender.com

# --- backend/ (Supabase-backed scaffold, separate service on its own port) ---
# Project Settings -> API in the Supabase dashboard.
SUPABASE_URL=https://gttsiljiyxrueuohhmsr.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imd0dHNpbGppeXhydWV1b2hobXNyIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODQ1MjkxNzksImV4cCI6MjEwMDEwNTE3OX0.mA3zIadbfNXDHMVeVMmOToYHwZXamwHsnUby101xWYc
# Project Settings -> Database -> Connection string -> URI. Used only to run
# schema DDL (CREATE TABLE IF NOT EXISTS ...) on server startup.
SUPABASE_DB_URL=postgresql://postgres.gttsiljiyxrueuohhmsr:Iconmobile%40liberty2026@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres